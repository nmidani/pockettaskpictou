kind = "web"
previewPath = "/"
title = "PocketTask"
version = "1.0.0"
id = "artifacts/pockettask"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 3000
name = "web"
paths = ["/"]

[services.development]
run = "npm run dev"

[services.env]
PORT = "3000"

[services.production]
build = ["npm", "run", "build:all"]
run = ["npm", "start"]
